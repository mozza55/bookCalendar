spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/bookcalendar
    username: sa
    password:
    driver-class-name: org.h2.Driver
#    data: classpath:db/data-h2.sql
#    initialization-mode: always


  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace